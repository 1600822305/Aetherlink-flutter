name: aetherlink_browser
description: "Aetherlink 内置浏览器能力包：HeadlessInAppWebView 会话（导航/正文提取/截图）+ SSRF URL 策略。见 docs/design/browser-tool-design.md。"
version: 0.0.1
homepage:

environment:
  sdk: ^3.9.2
  flutter: '>=3.24.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_inappwebview: 6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  assets:
    - assets/js/readability.js
    - assets/js/dom_snapshot.js
    - assets/js/run_helpers.js
