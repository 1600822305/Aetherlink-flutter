# 工作区功能缺口分析（Aetherlink-flutter · lib/features/workspace）

基于 main@cd802dc 的完整代码走读（文件树 / 多标签编辑器 / 终端 / 文件操作 / 后端能力层，共 ~13.4k 行）。
**2026-07-11 更新：标注了本轮已完成项（✅）与剩余项（⬜）。**

## 〇、本轮进度总览

已完成（按提交顺序）：

| 功能 | 提交 |
|---|---|
| ✅ 文件树搜索面板（文件名/内容/两者 + 正则，防抖、200 条上限） | 5941937 |
| ✅ 终端额外按键条（Esc/Tab/Ctrl/Alt/方向键长按连发/Home/End/PgUp/PgDn 等，粘滞 Ctrl/Alt） | 30b78a9 |
| ✅ 全局内容搜索：匹配行预览（渐进式）+ 点击跳转到文件对应行 | d89a65e |
| ✅ SSH 搜索远端加速（find/grep 一次往返，不可用自动回退 SFTP 遍历） | eeaf4b8 |
| ✅ 只读视图按行语法高亮（惰性、跨行状态、明暗主题、2 万行阈值） | 7ffd4f4 |
| ✅ `open(entry, line:)` 一等跳转 API（editorJumpProvider 上移 application 层） | 0e65dd1 |
| ✅ 多行 Tab 缩进/Shift+Tab 反缩进（修掉选区被替换 bug）、自动闭合配对、括号匹配高亮 | 48ae9f6 |
| ✅ 注释切换（Ctrl+/ + 工具条，按语言 // # -- REM） | 86180a8 |
| ✅ 外部修改冲突 diff 视图（双向对比 + 保留我的编辑/加载磁盘版） | 86180a8 |
| ✅ 文件树「显示隐藏文件」开关（默认隐藏、持久化） | fbdc8f7 |
| ✅ 文件树小功能包：长按菜单「复制路径/详情」+ 新建文件后自动打开 | c3890ac |
| ✅ 编辑器撤销/重做按钮 + 状态栏点击「跳转到行」 | 1f14b0b |
| ✅ 编辑器头部/工具条重构：功能按钮移入横向滚动工具条，头部编辑态开关 + 文件名/路径点击弹复制面板 | 1aa96ae + 见本提交 |
| ✅ 文件树排序（名称/修改时间/大小，目录优先，持久化） | 78de44c |
| ✅ Git 集成最小版：文件树 status 染色（M/A/U/D/R/C，目录滚包）+ 长按「Git 对比」只读 diff（限 canExec 后端） | ba76a91 |
| ✅ 终端小功能包：字体大小调节（持久化）+ 选区复制/粘贴按钮 + 输出 `path:line` 扫描跳转编辑器 | 见本提交 |

纯逻辑均落在 application 层 / `editor_edit_ops.dart` 等纯 Dart 模块，桌面端可直接复用。

## 一、现状总览（已经做得不错的）

- 三页横滑结构（文件树 / 编辑器 / 终端），会话自动恢复（工作区 + 打开的 tab + 活动 tab）
- 后端抽象干净：SAF / PRoot / SSH 统一接口，capabilities 门控 UI
- 文件树：懒加载 + 缓存、watch 事件去抖增量刷新、IDE 式"跟随活动文件"reveal、保护挂载点
- 编辑器：行号栏、当前行高亮、状态栏（行:列/选区）、查找替换（正则/大小写）、捏合缩放、Tab 缩进 + 自动缩进、外部修改冲突 banner、大文件/二进制/图片分级打开策略、虚拟化只读视图
- 文件操作：新建/重命名/移动/复制/删除，全部走目录选择器与确认对话框
- 终端：多 tab PTY、AI 会话接管视图

整体骨架是专业的，缺的主要是"小功能密度"和细节打磨。

## 二、从普通用户角度：缺失的小功能

### 文件树
1. ✅ ~~无文件名过滤/搜索框~~——已接入 `searchFiles`（文件名/内容/正则，5941937）。
2. ✅ ~~看不到隐藏文件开关~~——工具条眼睛按钮切换，默认隐藏、状态持久化（fbdc8f7）。
3. ✅ ~~无排序选项~~——工具条排序菜单（名称 A→Z / 修改时间新→旧 / 大小大→小，目录始终优先），渲染时排序无需重载，持久化；比较器在 application 层纯 Dart（`workspace_tree_sort.dart`），桌面端可复用。
4. ✅（部分）~~长按菜单缺"打开方式/详情"~~——已加「复制路径」（复制 readable 路径）与「详情」（名称/类型/大小/修改时间/路径，优先 getFileInfo 刷新）；只读后端也能用。**"用其他应用打开/分享"未做**（需原生分享通道）。
5. ⬜ **不能多选**——批量删除/移动/复制要一个一个来。
6. ✅（部分）~~新建文件无自动打开~~——创建后自动在编辑器打开；模板未做。
7. ⬜ **删除无撤销**——没有回收站/undo snackbar，SAF 上误删不可逆。
8. ⬜ **重名冲突体验**——move/copy 冲突只会抛错 toast，没有"覆盖/保留两者/重命名"选择。
9. ⬜ **文件图标很少**——只识别 dart/md/yaml/json/图片，js/ts/py/html/css/压缩包等都是通用图标。

### 编辑器
10. ✅ ~~无撤销/重做按钮~~——编辑态工具条「撤销/重做」按钮（共享 UndoHistoryController，canUndo/canRedo 联动置灰）。另：编辑开关移到头部锁定按钮旁并可随时退出（未保存时提示 保存/放弃/取消）；头部文件名/路径可点击弹面板一键复制。
11. ⬜ **保存无快捷路径**——dirty 状态下返回/切 tab 有守卫（好），但没有"全部保存"。
12. ✅ ~~无"跳转到行"~~——状态栏光标位置可点击，弹输入框跳到指定行（复用 `_jumpToLine`，只读/编辑态都支持，行号自动夹到有效范围）。
13. ⬜ **查找历史不保留**——每次打开 find bar 都是空的。
14. ⬜ **无最近文件列表**——tab 关掉就找不回，缺"最近打开"。
15. ⬜ **图片预览缺缩放信息/分享**；二进制/超大文件占位页没有"用外部应用打开"逃生口。
16. ⬜ **自动保存选项缺失**——手机上切走 app 容易丢编辑（虽然有 dirty 守卫）。

### 终端
17. ✅ ~~无额外按键行~~——已加 Esc/Tab/Ctrl/Alt/方向键（长按连发）/Home/End/PgUp/PgDn 工具条，粘滞 Ctrl/Alt（30b78a9）。
18. ✅ ~~无字体大小调节/复制粘贴按钮~~——额外按键条前置 A-/A+（持久化，8–28，普通 tab 与 AI 会话视图共享）、复制（长按选中后点复制）、粘贴（走 bracketed paste）。
19. ⬜ **无命令历史/常用命令收藏**。

## 三、从专业 IDE 角度：缺失的能力

### P0（对"IDE"定位影响最大）
1. ✅（部分）~~语法高亮完全没有~~——只读视图已按行高亮（7ffd4f4）；**编辑态 TextField 仍是单色**，需要自绘高亮编辑器（大工程，未做）。
2. ✅ ~~全局搜索（跨文件内容搜索）~~——搜索面板 + 匹配行预览 + 跳行（d89a65e），SSH 远端 grep 加速（eeaf4b8）。
3. ✅ ~~打开文件后跳转到指定行~~——`open(entry, line:)` 一等 API（0e65dd1）。

### P1
4. ✅ ~~括号匹配/自动闭合~~——自动闭合配对/包裹选区/跳过重复/退格删对 + 括号匹配高亮（48ae9f6）。
5. ✅（部分）~~多行 Tab 缩进~~——多行 Tab/Shift+Tab 整体缩进已修（48ae9f6）；**多光标/列选择未做**（需重写 TextField 编辑框架，成本极高）。
6. ✅ ~~注释切换~~——Ctrl+/ + 工具条按钮，按语言选前缀（86180a8）。
7. ✅（部分）~~文件对比/保存冲突~~——冲突双向 diff 视图 + 二选一（86180a8）；**逐 hunk 三方合并未做**（需 base/local/disk 三方与按块挑选 UI）。
8. ✅（部分）~~Git 集成缺失~~——最小版已落地：`git status --porcelain -z` 解析为纯 Dart（`workspace_git_status.dart`），文件树按状态染色 + 字母角标（目录滚包显示 •），长按菜单「Git 对比」弹只读 diff（HEAD vs 工作区，复用行 diff 组件）；仅 canExec 后端（SSH/PRoot/Termux）生效，刷新时机：打开工作区/手动刷新/变更事件去抖。**blame、暂存/提交操作未做**。
9. ⬜ **编码/换行符**——一律按 UTF-8 读写，状态栏不显示也不能切换编码（GBK 文件会乱码且保存会破坏），CRLF/LF 不显示不可切换。

### P2
10. ⬜ **符号导航/大纲**——无 outline、无 go-to-definition（可先做基于正则的当前文件符号列表）。
11. ⬜ **代码折叠**。
12. ⬜ **面包屑路径**——header 只显示截断的 readable path，不能点某一段跳转目录。
13. ⬜ **拖拽 tab 排序 / 拖拽文件到目录移动**。
14. ⬜ **编辑器设置持久化**——字体大小是每个 tab 的局部 state，换 tab/重进就丢；无 tab 宽度（2/4 空格）、无软换行开关（长行才被动降级 wrap）。
15. ✅（部分）~~终端与编辑器联动~~——按键条「跳转文件」扫描缓冲区的 `path:line`（纯 Dart 提取器 `terminal_output_links.dart`），弹列表选一个后相对路径解析到 workspace root，getFileInfo 换条目后跳编辑器对应行。**输出内直接点击、"在终端中打开所在目录"未做**。
16. ⬜ **任务/运行配置**——无常用命令快捷运行（可与 19 条合并做"命令收藏"）。

## 三点五、Git 集成完整版规划（未做，待立项）

最小版（status 染色 + 只读 diff）已落地，见 §三.8。完整版目标是 VSCode 源代码管理面板级别的能力，按性价比分阶段：

### 阶段 1：暂存 + 提交 + 推送闭环（最常用）
- ⬜ 变更列表面板：按暂存/未暂存分组展示 status 文件，点击看 diff（复用只读 diff 面板）
- ⬜ 暂存/取消暂存单个文件（`git add <file>` / `git restore --staged <file>`）与全部
- ⬜ 丢弃改动（`git restore <file>` / 未跟踪文件删除，需二次确认，不可恢复）
- ⬜ 提交：输入提交信息 + `git commit`；amend 上一条
- ⬜ 推送/拉取：`git push` / `git pull`，失败时展示 stderr（认证问题提示走 SSH 配置）

### 阶段 2：分支与历史
- ⬜ 分支列表/切换/新建/删除（`git branch` / `git switch`），头部显示当前分支 + ahead/behind
- ⬜ 提交历史查看（`git log` 列表 + 单提交 diff）；单文件历史
- ⬜ stash：保存/恢复/列表

### 阶段 3：精细化
- ⬜ 逐 hunk 操作：diff 面板按块「暂存这块/丢弃这块」（`git apply --cached` 或 `add -p` 脚本化）
- ⬜ blame：行级归属展示（编辑器 gutter 或单独面板，`git blame --porcelain`）
- ⬜ 编辑器行号栏改动标记（增/改/删色条，基于 `git diff -U0` 行区间）
- ⬜ 冲突解决 UI：merge/rebase 冲突文件逐块选择 ours/theirs（可与「三方合并」大工程合并立项）

### 实现约束（沿用最小版）
- 全部走 `WorkspaceBackend.exec`，限 canExec 后端（SSH/PRoot/Termux），SAF 不适用
- 命令输出解析放 application 层纯 Dart（沿 `workspace_git_status.dart` 模式），UI 在 presentation/mobile
- 路径参数一律 `shellQuoteArg` 引用；写操作（restore/丢弃/push）需确认对话框
- 每步操作后刷新 `gitStatusProvider`

## 四、剩余建议落地顺序（性价比排序）

1. ~~文件树小功能包：复制路径/详情菜单 + 新建后自动打开~~（已完成）
2. ~~编辑器：撤销/重做按钮 + 状态栏点击跳行~~（已完成）
3. ~~Git status 染色 + diff 查看~~（已完成）
4. ~~终端字体调节/复制粘贴按钮；终端 `path:line` 跳转~~（已完成，输出内直接点击未做）
5. ~~文件树排序~~（已完成）；删除软保护（不可恢复提示/undo）、重名冲突"覆盖/保留两者"
6. 编辑器设置持久化（字体大小/tab 宽度/软换行）
7. 编码/换行符显示与切换
8. 大工程（另行立项）：编辑态语法高亮、多光标/列选择、逐 hunk 三方合并、符号大纲/代码折叠
