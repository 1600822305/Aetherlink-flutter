# 智能体模式（Agent Mode）设计初稿

> 状态：P0 已全部落地，本稿随实现持续更新（拍板结论见 §九决策记录）。
> 前置阅读：《工作区与智能体模式-设计构想》（本稿是其阶段 B 的细化）。
> 产品设想（用户原话归纳）：**智能体是一个与聊天同级的全新主界面**，按钮互切；
> 退出时处于哪个模式，重启就回哪个模式（模式持久化）；能力对标
> Devin 云端 / Claude Code / Codex 这类可自主多步执行的智能体。

---

## 一、业界调研：主流智能体架构的共同骨架

对 Claude Code、Devin、OpenAI Codex（CLI/云端）、OpenHands（原 OpenDevin）、
Cursor Agent、Manus 等做横向拆解，尽管产品形态各异，核心架构高度趋同：

### 1.1 共同骨架：单线程主循环（Agent Loop）

所有主流智能体的核心都是一个极简循环，而不是复杂的多智能体编排：

```
while (未完成 && 未超限) {
  response = LLM(system prompt + 任务 + 历史事件流 + 工具定义)
  if (response 含工具调用) {
    result = 执行工具(可能先过审批)
    事件流.append(工具调用 + 结果)
  } else {
    完成 / 向用户提问
  }
}
```

- **Claude Code**：官方明确描述为"单线程主循环 + 工具"，复杂性全部下放到工具
  （文件读写、bash、搜索、子任务）而非编排层。
- **OpenHands**：`AgentController` 驱动 `step()` 循环，事件流（EventStream）是
  唯一事实源，所有 Action/Observation 都是事件。
- **Devin**：planner + 执行循环，长任务自动摘要 + 检查点，用户可随时介入。

**结论：不需要多 agent 编排框架。一个循环 + 好工具 + 好上下文管理 = 一线水平。**
这正好和本项目现状吻合：`chat_controller_streaming.dart` 里已经有一个
「LLM → 工具调用 → 结果回填 → 续跑」的循环（最多 N 轮），智能体引擎
本质是把这个循环从「消息树附属品」升级为「独立状态机驱动的长任务」。

### 1.2 六个关键机制（我们要逐个对标）

| 机制 | 业界做法 | 代表 |
|---|---|---|
| **计划/待办** | 显式 todo/plan 工具，模型自己维护任务清单，防长任务跑偏 | Claude Code TodoWrite、Devin planner、Manus 把 todo 重写进上下文尾部对抗"中间遗忘" |
| **上下文管理** | 事件流 + 自动压缩（compaction）：接近窗口上限时把旧事件摘要成一条，工具大输出截断落盘、按需回读 | Claude Code auto-compact、Devin 自动总结、OpenHands condenser |
| **权限分级** | 只读工具直通；写/执行工具过审批；用户可配置白名单（allowlist）；plan-mode 只读规划 | Claude Code permission modes + allowlist、Codex approval modes（suggest/auto-edit/full-auto）|
| **检查点/恢复** | 每步落库，进程死了可恢复；文件改动可回滚 | Devin snapshot、Claude Code checkpoint（v2）、OpenHands 事件流重放 |
| **子任务（subagent）** | 主循环派生一个独立上下文的子循环干专项活（如大范围搜索），只把结论带回主上下文，防止污染 | Claude Code Task/subagent、Devin 子会话 |
| **环境指令/技能** | 从工作区读 AGENTS.md/CLAUDE.md 注入系统提示；技能（skills）按需加载 | Codex AGENTS.md、Claude Code CLAUDE.md + skills |

### 1.3 对移动端的特殊约束（业界没有现成答案，需要自己定）

- **进程生命周期**：手机 App 会被切后台/杀进程，"用户离场也在跑"不能像云端那样
  假设进程常驻 → 必须 **每步落库 + 可恢复** 作为一等公民，前台服务保活为辅。
- **算力在别处**：真正跑命令靠工作区后端（SSH / Termux / PRoot），恰好本项目
  的 `WorkspaceBackend` 抽象已就位——agent 引擎在手机上只做"编排"，副作用
  全部发生在工作区后端里，天然接近"云端沙箱"模型。

---

## 二、现状盘点：能力层已经齐了八成

设计构想里说"共享能力层，隔离领域层/执行层/存储/任务态 UI"。逐项盘点现状：

| 智能体需要 | 现状 | 复用方式 |
|---|---|---|
| LLM 调用（流式、function calling） | ✅ provider 层 + `chat_controller_streaming` 的流式工具循环 | 抽出/仿写为 agent 引擎的一步（见 §四） |
| 文件读写工具 | ✅ `@aether/file-editor` 10 个工具（读 4 + 写 6，已从 14 个合并统一 API：write / edit（str_replace 唯一命中 + edits 多处原子替换）/ move 等），按 workspace root 隔离 | 直接复用 `ToolRoute` 分发 |
| 命令执行 | ✅ `@aether/terminal`（terminal_execute / terminal_session），命令三级风险评级 | 直接复用 |
| 知识库/记忆/网搜 | ✅ `@aether/knowledge`、search_memory、builtin_web_search | 直接复用 |
| HITL 审批 | ✅ `tool_confirmation_service`（60s 超时、5 分钟宽限窗）+ **工具授权白名单**（刚落地，持久化、越界命令硬约束） | 直接复用，agent 侧加"运行级审批策略"（见 §七） |
| 技能 | ✅ skills 商店 + `read_skill` 工具 | 直接复用 |
| 上下文压缩 | ✅ `context_condense_service`（聊天已有压缩） | 改造为事件流 compaction |
| 工作区 | ✅ 文件树/编辑器/多会话终端/SSH/Termux/PRoot/SAF | agent 的"手术台"，只依赖 `WorkspaceBackend` |
| 计划/待办工具 | ❌ 无 | 新增（P0，很小） |
| 任务状态机 + 事件流存储 | ❌ 无 | 新增（P0 核心） |
| 检查点/恢复 | ❌ 无 | 事件流落库天然获得（P0） |
| 子任务 | ❌ 无 | P2 |
| diff/产物面板 | ⭕ 有只读 git diff 染色 | P1 增强 |

**结论：新做的东西 = 领域层（状态机+事件流+存储）+ 执行引擎（把现有工具循环
从消息树里解放出来）+ 任务态 UI + 模式切换壳。工具层几乎零新增。**

---

## 三、模式切换与持久化（用户设想的入口形态）

### 3.1 形态

- 智能体是**全新的主界面**（`/agent`），与聊天主界面（`/`）**同级**，不是聊天里的一个开关。
- 互切入口：两个主界面的顶栏（或侧栏底部，与「工作区」「翻译」入口同排）放
  模式切换按钮；聊天 → 智能体、智能体 → 聊天。
- **模式持久化**：切换时写 `appSettingsStore`（key: `app_main_mode` = `chat` | `agent`）；
  冷启动时读取该值决定初始主界面（GoRouter `redirect` 或 `initialLocation`）。
  退出前在智能体模式 → 重启进智能体模式，反之亦然。

### 3.2 路由

```
/          聊天主界面（现状不动）
/agent     智能体主界面：当前话题的任务工作台 + 侧边栏（新）
           侧边栏两级：智能体档案 / 话题（对齐聊天的 助手/话题）
```

agent 模式重启时除了模式本身，还恢复 last-active 智能体与话题。

聊天现有的全部子路由（设置、工作区、笔记……）保持不变；智能体页可直接
push 既有页面（工作区、设置）——设置/工作区本来就是"产品级"页面，两个模式共用。

---

## 四、领域模型与状态机（`features/agent/`，与 chat 完全隔离）

### 4.1 实体

```dart
/// 智能体档案 = 可配置的专长角色（对齐聊天的助手）：
/// 「编程」「网络研究」「PPT/文档」……内置预设 + 用户自建
class AgentProfile {
  String id;
  String name; String emoji;
  String systemPrompt;          // 专长与行为风格
  Set<AgentToolGroup> tools;    // 可用工具集：文件/终端/网搜/知识库/技能
  String? workspaceId;          // 绑定工作区（一个工作区对应一个智能体，
                                // 在智能体设置里改；话题直接继承，见 §九.2）
  AgentPermissionMode defaultPermissionMode; // P1
  bool builtin;                 // 预设不可删可复制
}
// 设置页复用聊天助手设置页（共用项），顶部切换进「智能体专用」页
// （工具集/绑定工作区/权限）；实现时评估 AgentProfile 复用 Assistant
// 的共用字段还是独立实体+引用，以复用设置组件成本最低者为准。

/// 一个话题（任务）= 某智能体下的一次委托（对标 Devin session）
class AgentTask {
  String id;
  String profileId;        // 归属的智能体档案
  String title;            // 首条指令自动生成，可改
  String? workspaceId;     // 创建时继承自档案的绑定工作区（§九.2）
  String modelId;          // 执行模型
  AgentTaskStatus status;  // 状态机，见 4.2
  AgentPermissionMode permissionMode; // 见 §七
  DateTime createdAt, updatedAt;
}

/// 事件流：任务内发生的一切，append-only（对标 OpenHands EventStream）
/// 一条事件 = 一个 sealed 变体：
///   UserMessageEvent      用户指令 / 追加指令 / 审批答复
///   AssistantTextEvent    模型文字（思考/汇报，流式）
///   ToolCallEvent         工具调用（名称+参数+审批状态）
///   ToolResultEvent       工具结果（大输出截断，全文落盘引用）
///   PlanUpdateEvent       计划/待办更新（update_plan 工具产生）
///   CompactionEvent       上下文压缩摘要（替代其覆盖的旧事件参与后续上下文）
///   StatusChangeEvent     状态迁移记录（含原因：出错/用户暂停/超限……）
class AgentEvent { String id; String taskId; int seq; ...payload; }

/// 产物：任务产出的可展示结果（P1）
class AgentArtifact { String id; String taskId; ArtifactKind kind; ... }
// kind: fileDiff / fileSnapshot / commandOutput / summary
```

**不复用 chat 的 `Message/MessageBlock`**：消息树是"对话"模型（分支/重发/多模型），
事件流是"执行审计"模型（append-only、seq 严格递增、可重放）。两者语义不同，
强行复用会把 chat 的复杂度（版本树、块状态机）拖进来。构想文档的隔离结论维持。

### 4.2 任务状态机

```
draft（空白新话题，对齐聊天「新的对话」，发首条消息才启动）
  │
queued ──▶ running ──▶ done
             │ ▲          
             │ │ resume    
             ▼ │          
   ┌─ waitingApproval（HITL 审批中，60s 不答复自动 paused 而非拒绝*）
   ├─ waitingInput   （模型主动向用户提问）
   ├─ paused         （用户手动暂停 / App 被杀后恢复为 paused）
   ├─ failed         （连续错误超限 / 模型不可用）
   └─ cancelled      （用户终止）
```

\* 与聊天不同：聊天里审批超时=拒绝并继续；agent 长任务里用户可能离场，
超时应**挂起任务**等用户回来，而不是替用户做决定。

### 4.3 存储（新 drift 表，独立于 chat 表）

```
agent_profiles(id, name, emoji, system_prompt, tools_json, workspace_id, ...)
agent_tasks   (id, profile_id, title, workspace_id, model_id, status, permission_mode, ...)
agent_events  (id, task_id, seq, kind, payload_json, created_at)
              -- 索引 (task_id, seq)；payload 大字段（工具全量输出）超过阈值
              -- 落应用私有目录文件，行内只存引用，防表膨胀
agent_artifacts (P1)
```

每个事件产生即落库 → **检查点免费获得**：App 被杀后任务变 paused，
重进时从事件流重建上下文继续跑（业界"事件流重放"模式）。

---

## 五、执行引擎（`features/agent/application/agent_engine.dart`）

### 5.1 主循环

```dart
while (task.status == running) {
  context = 组装上下文(系统提示, 事件流);        // §5.2 / §5.3
  response = await llm.stream(context, tools);   // 复用 provider 层
  for (toolCall in response.toolCalls) {
    if (需要审批(toolCall) && !白名单命中) {      // 复用 tool_auth_policy
      task.status = waitingApproval; 等用户;      // UI 推送审批卡片
    }
    result = await 执行(toolCall);               // 复用 ToolRoute 分发
    events.append(ToolCallEvent, ToolResultEvent);
  }
  if (response 无工具调用) {
    是收尾汇报 → done；是提问 → waitingInput;
  }
  安全阀: 轮数/token/连续失败 超限 → paused + 说明;
}
```

与聊天工具循环的三个关键差异：

1. **宿主不同**：不挂在消息树上，挂在 `AgentTask` 事件流上；
2. **审批语义不同**：超时挂起而非拒绝（§4.2）；
3. **无限轮**：聊天循环有固定最大轮数保守值；agent 以预算制
   （token 预算 + 轮数上限 + 连续失败上限）替代，超限挂起让用户续。

实现上**不改动** `chat_controller_streaming.dart`；把工具执行段
（route 分发、审批、取消信号）中可共享的纯逻辑下沉到共享 helper，
聊天与 agent 各自调用——避免"为了复用把两个模式焊死"。

### 5.2 系统提示：分层组装（已拍板，对标 Claude Code / Codex）

用户提示词**不是**整个系统提示（区别于普通聊天），只作为单独一节追加。
自上而下五层拼装，层级越靠前权威越高：

```
[1 内置基础指南]   固定内置、随版本迭代、用户不可改。干活纪律最小集：
                  自主多步、先探索后修改、小步验证、用 update_plan 维护
                  计划、何时该问用户、工具使用规范、安全边界。精简省 token。
[2 环境上下文]     运行时生成：工作区 root/文件树摘要（复用 workspaceContext）、
                  平台信息、当前可用工具清单（按档案工具集过滤）。
[3 档案专长段]     AgentProfile.systemPrompt——预设（编程/网络研究/PPT）的
                  专长描述就存这里。
[4 用户附加指令]   用户在设置里写的提示词只进这一节：可定制风格/偏好，
                  不能覆盖基础指南与安全边界。
[5 项目指令]       工作区根有 AGENTS.md 则注入（对标 Codex/Claude Code）★新增
[技能索引]         细节流程不进系统提示，全部用内置 skills 承载（如「PPT
                  制作流程」「调研方法」），指南里只放技能索引，模型按需
                  read_skill 加载；预设档案 = 专长段 + 默认技能组合。
```

工具定义本身走 function calling 的 tools 参数（复用 McpSetup 组装），
不占系统提示正文。

### 5.3 上下文管理（长任务的命门）

1. **工具大输出截断**（已落地）：结果超 8000 字符 → 头 4000/尾 2000 保留、
   砍中间，全文落应用文档目录 `agent_tool_outputs/`，回填文本附落盘路径
   并提示模型用 `read_file` 按行范围回读（Manus/Claude Code 模式）；
   上下文与事件库只存截断版，详情面板「查看全文」读同一落盘文件；
2. **自动 compaction**（已落地）：事件流上下文字符量（token 粗代理）超触发
   阈值（默认 120k 字符，保留最近 40k）时，把最老的一段事件用 LLM 摘要成
   一条 `CompactionEvent`，重放时用摘要替代被覆盖段，原事件仍在库里
   （审计不丢），只是不再进上下文；
3. **计划置尾**：每轮把当前 todo/plan 附在上下文末尾，对抗长上下文"中段遗忘"。

### 5.4 新增的 agent 专属工具（很小，都是纯逻辑）

- `update_plan`：模型维护待办清单（写 `PlanUpdateEvent`，UI 实时渲染）。
  已拍板对标 Claude Code TodoWrite / Devin todo 同款：参数为**全量覆盖**的
  清单数组 `[{content, status}]`，status 三态 pending/in_progress/completed，
  纪律（写进基础指南）：多步任务必须先列计划、每完成一项立即更新、
  同一时刻只保留一项 in_progress；
- `ask_user`：显式向用户提问并挂起（区别于"文字里顺嘴一问"）；
- `finish_task`：显式收尾 + 结果摘要（驱动状态机到 done）。

### 5.5 生命周期与后台

- P0：**前台可跑 + 杀进程可恢复**。引擎跑在 Dart 侧，App 在前台/最近任务里
  正常执行；进程死亡 → 任务标 paused，重进一键"继续"。
- P1（已落地）：Android 前台服务（`foregroundServiceType=dataSync`）+
  常驻通知，切后台继续跑——复用 chat 流式保活的
  `StreamingKeepAliveService`，改造为持有方引用计数
  （chat/agent 共用一个服务，全部释放才停）；task runner 首个任务
  启动时 acquire、运行集清空时 release。网络型工作区（SSH）断连
  自动重连恢复会话待做。
- 不做云端执行（超出 App 边界）。

---

## 六、任务态 UI（`features/agent/presentation/mobile/`）

### 6.1 智能体主界面（`/agent`）

主界面 = 当前话题的任务工作台；侧边栏（SidebarHost 同款）两级：
智能体档案列表 → 当前档案的话题列表（进行中置顶/历史倒序），
底部「回聊天」模式切换。详细线框见《智能体模式-UI设计稿-移动端》。
新建话题 = 选工作区（默认继承档案）+ 选模型 + 输入指令 → 进工作台开跑。

### 6.2 任务详情页（`/agent/task/:id`，核心页面）

三段式（对标 Devin 会话页）：

1. **计划区**（可折叠）：当前 todo 清单，完成项打勾——`PlanUpdateEvent` 驱动；
2. **事件流**：按 seq 渲染——模型文字气泡 / 工具卡片（复用现有
   工具块视图：run_command_block、diff 块、terminal_session 块的展示件）/
   审批卡片（批准/拒绝/加入白名单，复用确认服务 UI 语义）/ compaction 分隔线；
3. **输入条**：追加指令（排队为 `UserMessageEvent`，下一轮生效）、
   暂停/继续/终止按钮。

### 6.3 审批体验

- 任务详情内嵌审批卡；App 在后台时发系统通知（已落地：
  `AgentNotificationService`，审批挂起且 App 不在前台时弹高优通知，
  点击回 App 并选中对应话题；裁决/顶替时自动撤下；前台不打扰）；
- 审批卡上提供"本任务内不再询问此工具"（运行级宽限）与
  "永久加入白名单"（写入刚做的 `tool_auth_policy`）两档快捷入口。

---

## 七、权限与安全（在现有基座上加"运行级策略"）

现有三层不动：只读直通 → 写/执行 HITL → **工具授权白名单**（持久化）→
越出工作区 root 的终端命令**永远强制审批**（硬约束，白名单/宽限均不可覆盖）。

agent 侧新增**每话题的模式**，输入条上沿快切（已拍板，对标 Cursor
Code/Ask/Plan 与 Codex approval modes，详见 UI 稿输入区）：

| 模式 | 行为 |
|---|---|
| `plan`（Plan，只读规划） | 只允许读类工具；先出完整方案，确认后一键转 code 继续执行（已落地，见决策 25） |
| `ask`（Ask，只问答） | 仅只读工具，不改任何东西，用于问代码/问方案 |

Ask/Plan 的只读约束是**双层硬约束**（已落地，Roo Code 同款方案）：
① 源头不暴露——按 task.mode 组装工具目录，写类工具与终端组直接从发给
模型的 definitions 里剔除（顺带省十几个写工具 schema 的上下文 token）；
② 执行层兜住幻觉调用——不在工具集内的调用由执行器硬拒。
| `code`（Code，默认） | 执行模式：写/执行过审批，白名单命中免审 |
| `auto`（放手跑，已落地） | 任务绑定工作区 root 内的写/执行免审直通；越界仍强制审批 |

`auto` 模式已落地（见决策 26）：切入时弹二次确认，状态条/输入区 chip
常驻琥珀色 AUTO 徽标。免审范围是严格的白名单式判定：
- 文件工具：调用携带的全部路径参数（path/parent_path/source_path/
  destination_path）都落在绑定工作区 root 内才免审，无路径参数按越界处理；
- 终端：仅 terminal_execute 且 workspace 参数明确指向绑定的 project
  工作区、命令风险评级非越界才免审；terminal_session 写入、未指定/
  指向其它工作区一律照常审批；
- 未绑定工作区的任务不产生任何免审；越界命令硬约束不变。
所有工具调用无论是否免审都写事件流 → 完整审计。

---

## 八、分期计划

### P0 —— 能跑通一个真任务（最小闭环）
1. 领域层 + drift 表 + 事件流（§四）；
2. 执行引擎主循环 + update_plan/ask_user/finish_task 工具 + 预算安全阀（§五）；
3. 复用现有工具链与审批白名单，审批超时挂起语义；
4. `/agent` 任务列表 + 任务详情（计划区/事件流/输入条）；
5. 模式切换按钮 + 模式持久化（§三）；
6. 杀进程恢复（事件流重建 + 一键继续）。

（P0 全部完成；下列 P1 中 7、9 已完成，Ask/Plan 只读硬约束也已落地。）

### P1 —— 好用
7. ✅ 自动 compaction + 工具大输出截断落盘回读；
8. ✅ `auto` 模式（plan/ask/code 在 P0 随模式快切落地；auto 见 §七
   与决策 26）；
9. ✅ AGENTS.md 环境指令注入；
10. ✅ diff/产物面板 + 改动文件清单（工作台 diff tab：git status 未提交
    改动清单 + 只读 diff 面板，经 `app/di/agent_workspace_access.dart`
    seam 取 git 能力）；
11. ✅ Android 前台服务后台跑 + 审批系统通知（见 §5.5 / §6.3）。

### P2 —— 对标一线
12. 子任务（独立上下文的子循环，结论回传）；
13. 任务模板/定时任务；多任务并行（每任务独立终端会话池已具备）；
14. ✅ 检查点回滚（git 基线，见决策 27）。

---

## 九、决策记录（全部已拍板；每拍板一项立即更新本节并推 main，防跨会话失忆）

1. **模式切换按钮位置**：侧栏底部，与「工作区」「翻译」同排；
   智能体主界面侧栏底部放"回聊天"。
2. **工作区绑在智能体上**（替代早期"话题级强制选工作区"）：一个工作区
   对应一个智能体，在智能体设置里改；新建话题对齐聊天「新的对话」：
   点新建立即创建一条空白草稿话题（status=draft）占位到列表并选中，
   已有空白草稿直接复用；发第一条消息才定标题并启动引擎，工作区
   直接继承档案，不单独选、不填任务表单；纯调研需求直接用普通聊天。
3. **执行模型选择**：复用普通聊天的模型选择，后期有需求再单独新增。
4. **iOS**：只保留 SSH 工作区后端跑命令（无 Termux/PRoot）。
5. **信息架构**：侧边栏两级：智能体档案（对齐助手，可配置提示词/工具集/
   绑定工作区，实现编程/网络研究/PPT 等不同专长）/ 话题（=一次任务执行）。
6. **智能体设置页**：复用聊天助手设置页共用项，顶部切换进「智能体专用」
   独立页（工具集勾选/绑定工作区/权限偏好）。
7. **系统提示**：五层分层组装（§5.2）——内置基础指南 → 环境上下文 →
   档案专长段 → 用户附加指令（仅一节）→ AGENTS.md；细节流程走内置 skills。
8. **事件流形态**：Devin 式工作段折叠摘要块 + Claude Code 式执行中实况
   （UI 稿 §4.1）。
9. **打断交互**：输入框状态驱动，软硬两档（有文字→发送三选面板：排队/
   立即打断并发送/继续编辑；无文字→暂停/长按强制终止）（UI 稿 §五）。
10. **输入区**：上沿 chips=模式快切（Code/Ask/Plan，对应权限模式）+
    模型选择（不放顶栏）；＋按钮弹附件面板（UI 稿输入区）。
11. **任务工作台**：两页横滑（左事件流含计划纪要面板；右工作台：终端/
    AI 正在看的文件/diff tab），非三页。
12. **update_plan**：Claude Code TodoWrite / Devin todo 同款——全量覆盖
    数组、三态、单项 in_progress（§5.4）。
13. **实施顺序**：UI 先行（mock 事件流数据把全部界面跑通）→ 再接真引擎/
    drift 持久化；代码全部收在 `features/agent/` 独立模块，绝不 import
    features/chat 内部，chat 侧只加模式切换按钮+路由；import 边界测试加
    agent↔chat 互不引用规则。
14. **防失忆机制**（产品内）：话题事件流全量落库，切话题/杀进程/重启均可
    重放恢复；todo=PlanUpdateEvent 持久化即进度跟踪；compaction 只影响
    模型上下文，原始事件永久可回看。
15. **App 级设置页打通**：复用普通聊天的设置页（设置 hub），顶栏加一个
    切换入口（bot/messageSquare 图标）在「聊天设置 ↔ 智能体设置」间切换；
    智能体侧正文由 agent 模块提供（`AgentAppSettingsBody`），两侧完全独立
    互不影响。结构对齐聊天设置：目录 + 多级子页。**智能体该有哪些设置项
    尚未设计，当前只留空骨架占位，不预设任何分组/行**，定稿后再填。
    智能体侧边栏设置 tab 底部加「设置」入口行，直达设置页的智能体视图
    （`/settings?mode=agent`）。
16. **用户消息形态**：用户指令渲染为**贴右对称气泡**（Devin/IDE 同款），
    与 agent 侧时间线叙述（贴左、无气泡）区分，一眼分清谁说的；其余事件
    维持左轨时间线形态（§8 不变）。
17. **循环设计 L1–L8 全部按建议拍板**（详见《智能体循环-架构调研与设计》
    §3.3）：显式 finish_task/ask_user 出口 + 无工具调用兜底；拒绝回填继续
    跑、审批挂起无超时；用户插话 queued 事件安全点消费；协作式取消工具
    边界生效；预算默认轮数 50/连续失败 5/单工具 5min 超限 paused；流式
    文本原位 upsert 不逐 chunk 追加；重启 running→paused、继续=半途工具
    按失败回填后重放续跑；子任务 P0 不做但内核不写死单任务假设。
    落地顺序按该稿 §3.4 五步，每步独立推 main（五步已全部完成）。
18. **Ask/Plan 只读硬约束**：双层方案（§七）——源头不暴露写类工具 +
    执行器硬拒兜底；不做 Codex 式 OS 沙箱（Android 上不可行）。
19. **工具大输出截断落盘**：8000 字符阈值、头 4000/尾 2000、全文落
    `agent_tool_outputs/`，模型可 `read_file` 回读，详情面板「查看全文」
    读同一文件（§5.3）。
20. **工作区工具 14→10 合并统一 API**：write（写入即创建）、edit
    （str_replace 唯一命中 + edits 多处原子替换，替代 insert_content/
    apply_diff/replace_in_file）、move（含重命名）、list_files（合并
    get_workspace_files）；read_file 加长行截断与 60k 字符上限；旧工具名
    保留渲染兼容历史消息。
21. **基础指南加入极简沟通风格约束**（§5.2 层 1）：极度压缩输出、不写
    前言后语、不复述工具输出、完成任务简短确认、不用表情不谎报成功。
22. **流式增量落库限流**：LLM SSE 增量经 _StreamingEventWriter
    latest-wins 合并、至多每 200ms 落库一次（修长任务 UI 冻结/ANR），
    收尾语义不变。
23. **新建话题对齐聊天**：新增 AgentTaskStatus.draft，点新建立即建空白
    草稿话题占位并选中（已有空白草稿复用），发首条消息才定标题启动
    引擎；话题列表按最近活跃（updatedAt）降序（见决策 2 更新）。
24. **事件流体验打磨**（UI 稿同步）：助手叙述接聊天同款 Markdown 渲染
    （流式块级记忆化）；粘底跟随滚动 + 「回到最新」按钮；工作段默认
    折叠（侧栏设置项控制）、段头动词摘要 + 耗时/操作数/±行数；工具
    详情面板固定 2/3 屏高、参数/输出内部滑动；面板关闭不自动聚焦
    输入框；话题卡去掉最近事件摘要行，状态只靠状态灯。
25. **Plan→Code 一键转**：切换权在用户，AI 不能自切模式（否则只读
    约束形同虚设）——Plan 任务收尾（done）时事件流末尾出「方案已就绪」
    卡，点「转 Code 执行」= 切 code 模式 + 注入确认消息续跑（切模式出
    StatusChangeEvent 审计）；需要改方案直接在输入条继续讨论。顺带
    接通输入区 chips 中途切模式：非执行中发消息携当前 chip 模式同步
    到话题（此前只改本地态不生效）。
26. **auto 模式落地**：免审判定收在 composition root 的审批门
    （`_PolicyApprovalGate`），不新建授权系统——task.mode==auto 且调用
    可靠落在任务绑定工作区 root 内才 allow，其余照走原审批链
    （白名单/运行级宽限/审批卡不变）。边界判定用共享 helper：
    `fileEditorPathsWithinRoot`（全部路径参数 boundary-aware 包含）与
    `terminalCommandStaysInBoundRoot`（workspace 参数必须指向绑定
    project 工作区且命令风险非越界）。二次确认放输入区模式快切
    （选 Auto 弹确认，取消保持原模式）；默认模式设置不提供 auto
    （保证进 auto 必经确认）；AUTO 徽标（琥珀色）挂状态条与输入区
    chip。system prompt 提示模型带 workspace 参数以命中免审。
27. **检查点回滚（git 基线）**：调研 Claude Code（工具级备份，终端
    改动不覆盖）/ Cline（影子 git，最完整）/ Cursor（轮次 diff 快照）
    后拍板走 git 基线就地变体：code/auto 模式每条用户消息落地前，用
    临时索引（GIT_INDEX_FILE）把工作区全量状态（含未跟踪，尊重
    .gitignore）写成 commit 挂到 `refs/aetherlink/checkpoints/任务/时间戳`
    防 gc，不碰用户 HEAD/index/分支；事件流落 CheckpointEvent 标记行
    （不进模型上下文），行内「回滚」按钮仅非运行态可用，确认后先把
    当前状态落为新检查点（可再回滚回来），再删掉检查点后新增文件 +
    `git restore --source` 整树还原（终端改动也覆盖），只还原文件不动
    对话；回滚出 StatusChangeEvent 审计。git 能力经 app/di 的
    agent_checkpoint_access seam 取。SAF 不硬限制但不支持（无法执行
    git），非 git 工作区提示初始化 git 后可用；不可用提示每个任务只出
    一次（StatusChangeEvent），不阻断任务。非 git 工作区的工具级备份
    兑底留待后续单做。
