# 工具动态装载（渐进披露）——实施计划

> 状态：计划（未实施）。本文档为下个会话的实施交接。
> 背景：mcp_manage（PR #752）已落地「最小 schema 常驻 + 正文进 skill」的
> 形态；本计划把这套渐进披露推广为通用机制——**工具组默认不进 tools
> 列表，模型 read_skill 读取对应技能后，下一轮才注入该组工具定义**。
> 对标 Claude Code 的 deferred tool loading。

## 1. 目标与非目标

### 目标

- 常驻上下文只保留核心工具（控制类、read_skill、文件/终端等高频组），
  低频大组（首批试点：浏览器 11 个工具）延迟装载。
- 模型保留完整 schema 校验：装载后 tools 列表里是原样定义，
  不走「JSON 字符串转发」的 bridge 路线。
- 激活状态可恢复：任务续跑 / 应用重启后重放，已激活的工具组不丢。

### 非目标

- 不改变审批/权限模型（路由与 HITL 判定不感知装载状态）。
- 不动聊天（chat）侧的工具装配，先只做智能体（agent）侧。
- spawn_subagent、mcp_manage 本来就是单工具+skill，保持现状。

## 2. 现状关键事实（已核实，2026-07）

- tools 列表**每轮请求都重新发送**：`_GatewayAgentLlmClient`
  持有 `List<McpToolDefinition> _definitions`，每次
  `LlmChatRequest(tools: _definitions, ...)`（
  `lib/app/di/agent_runtime_access.dart`）。列表是可变实例，且与
  `forProfile` 装配的 catalog 共享——**中途改动这个 List，下一轮请求
  即生效**，这是本方案可行的根基。
- catalog 在 `AgentRuntime.forProfile` → `_catalogFor` 一次性构建：
  `definitions`（发给模型）与 `routes`（执行分发 Map）同源。
- `read_skill` 经 `SkillReadToolRoute` 在 `ChatToolExecutor` 内执行
  （`lib/shared/mcp_tools/skill_read_tool.dart` 的 `executeReadSkill`）。
- 浏览器组当前在 `_catalogFor` 里按 `AgentToolGroup` 全量注入
  `kBuiltinMcpTools['@aether/browser']` 的 11 个定义。
- 内置技能「内置浏览器」（`builtin-browser`，
  `lib/shared/config/builtin_skills/browser_skill.dart`）已包含完整用法。
- 系统提示已注入「可用技能」清单（read_skill 按名称读取）。

## 3. 设计

### 3.1 核心抽象：DynamicToolCatalog

新建（建议放 `lib/features/agent/application/engine/` 或
`lib/app/di/`，注意 agent↔chat 架构隔离——组装在 app/di 最稳）：

```dart
class DynamicToolCatalog {
  final List<McpToolDefinition> resident;      // 常驻定义
  final Map<String, List<McpToolDefinition>> deferred; // skillId → 定义组
  final Map<String, ToolRoute> routes;         // 全量路由（不裁剪！）
  final Set<String> activated = {};            // 已激活 skillId

  List<McpToolDefinition> definitionsForTurn() =>
      [...resident, for (final id in activated) ...deferred[id]!];

  void activate(String skillId) { activated.add(skillId); }
}
```

要点：

- **routes 永远全量**。routes 不发给模型，只用于执行分发与重放
  （`_replayMessages` 里历史工具结果要能找到路由）；只裁剪 definitions。
- `_GatewayAgentLlmClient` 改为持 `DynamicToolCatalog`，每轮
  `tools: catalog.definitionsForTurn()`。
- 审批门 `_PolicyApprovalGate` 继续用全量 routes，零改动。

### 3.2 激活时机：read_skill 执行成功后

在 read_skill 的执行路径上挂钩（两个选点，推荐 A）：

- **A（推荐）**：`_McpAgentToolExecutor.runTool` 分发处——工具名为
  `read_skill` 且结果非 error 时，按返回的技能解析 skillId，调
  `catalog.activate(skillId)`。组装点在 app/di，天然拿得到 catalog。
- B：改 `executeReadSkill` 返回结构带 skillId，由调用方激活。
  改动面大，不必要。

注意 read_skill 的名称匹配是「精确 → 忽略大小写 → 子串」三段式，
激活判定必须复用同一匹配逻辑（抽出共用函数），避免读到了技能但
没激活工具的错位。

### 3.3 激活状态恢复（重启/续跑）

不加新存储。任务重放时（`_replayMessages` 同路径）扫描事件流：
凡出现 `read_skill` 工具事件且执行成功，按 3.2 同款匹配逻辑重新
`activate`。事件流 append-only，天然是激活记录。

### 3.4 模型引导

- 常驻上下文的「可用技能」清单里，对绑定了工具组的技能加一句：
  「读取本技能后，对应工具将在下一轮可用」。
- 被延迟的组在常驻里**不留占位定义**（CC 同款）；模型看到任务需要
  浏览器 → 从技能清单知道有「内置浏览器」→ read_skill → 下轮工具出现。
- 技能正文开头补一行：「本技能对应的工具已随本次读取装载，
  下一轮起可直接调用」。

### 3.5 绑定关系声明

在内置技能定义上加可选字段（`Skill` 已是 freezed，加字段跑
build_runner），或先用 app/di 里的常量 Map 起步（改动最小，推荐先这样）：

```dart
const kSkillToolBindings = {
  'builtin-browser': '@aether/browser', // skillId → builtin server name
};
```

## 4. 分步实施（建议一个 PR 一步）

1. **PR-A 机制 + 浏览器试点**：
   - DynamicToolCatalog；`_catalogFor` 拆 resident/deferred；
     浏览器组进 deferred（`kSkillToolBindings`）；
   - `_GatewayAgentLlmClient` 每轮取 `definitionsForTurn()`；
   - read_skill 执行挂钩激活 + 重放恢复；
   - 技能清单/正文提示语；
   - 测试：`test/features/agent/`——激活前 tools 无浏览器定义、
     read_skill 后有、重放后仍有、routes 始终全量、审批不受影响。
2. **PR-B 扩围**（视效果）：评估把 knowledge 组（kb_*）、web_search
   等低频组也 deferred；子代理与 mcp_manage 维持单工具常驻。

## 5. 风险与对策

- **模型不读技能就想调工具** → 首轮会报「未知工具」；工具错误信息里
  提示「先 read_skill 读取对应技能」。（引擎对未知工具已有兜底回填，
  确认错误文案可定制。）
- **子代理**：子代理有独立 catalog，若子代理类型预设需要浏览器，
  直接把该组放 resident（子代理上下文短，无需渐进披露）。
- **Ask/Plan 只读模式**：deferred 组同样要过 readOnly 过滤——
  `definitionsForTurn()` 输出后按现有 readOnly 规则再滤一遍即可。
- **token 收益量化**：实施前先打印浏览器 11 个定义 JSON 的字符量，
  写进 PR 描述作为依据。

## 6. 验证清单

```bash
dart run build_runner build -d       # 若改了 @freezed/@riverpod
flutter analyze <改动文件...>
flutter test test/features/agent
flutter test test/shared/mcp_tools/
```

（test/architecture 的 agent↔chat 违规为 main 存量失败，与本计划无关。）
